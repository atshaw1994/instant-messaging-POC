<div class="container mx-auto p-8">
  <h1 class="text-4xl font-bold mb-4 text-center">
    Welcome to the Instant Messaging POC
  </h1>
  <p class="mb-8 text-center text-gray-600">
    This is a proof of concept for an instant messaging application using Ruby on Rails and Devise.
  </p>

  <% if user_signed_in? %>
    <div class="text-center">
      <p class="text-xl mb-4">You are logged in as **<%= current_user.email %>**.</p>
      
      <%# 1. Link to view chats %>
      <%= link_to "View My Chats", chats_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full inline-block mr-2" %>
      
      <%# 2. Link to log out %>
      <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-full inline-block" %>
    </div>

  <% else %>
    <div class="text-center space-x-4">
      <p class="text-xl mb-4">Please sign in or sign up to continue.</p>
      
      <%# 3. Link to sign up (registration) %>
      <%= link_to "Sign Up", new_user_registration_path, class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-6 rounded-lg shadow-lg" %>
      
      <%# 4. Link to log in (session) %>
      <%= link_to "Log In", new_user_session_path, class: "bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-6 rounded-lg shadow-lg" %>
    </div>
  <% end %>
</div>